import React from "react";
import styles from "./Portfolio.module.scss";

const Portfolio = () => {
  return <section className={styles.portfolioSection}>Portfolio</section>;
};

export default Portfolio;
